<?xml version="1.0" encoding="utf-8"?>
<MotionScene xmlns:android="http://schemas.android.com/apk/res/android"

    xmlns:motion="http://schemas.android.com/apk/res-auto">
    <!--Transition：
     constraintSetStart：开始的位置
     constraintSetEnd：结束的位置
     autoTransition： 动画结束的位置
     motionInterpolator:定义缓动动画
     pathMotionArc:两点之间的path形状 也就是路径形状
     -->
    <Transition
        motion:constraintSetEnd="@+id/end2"
        motion:constraintSetStart="@+id/start2"
        motion:duration="1000">
        <OnSwipe
            motion:dragDirection="dragRight"
            motion:onTouchUp="stop"
            motion:touchAnchorId="@id/button3"
            motion:touchAnchorSide="right" />
    </Transition>

    <ConstraintSet android:id="@+id/start2">
        <Constraint android:id="@id/button3">
            <Layout
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:layout_marginStart="8dp"
                motion:layout_constraintBottom_toBottomOf="parent"
                motion:layout_constraintStart_toStartOf="parent"
                motion:layout_constraintTop_toTopOf="parent" />
        </Constraint>
    </ConstraintSet>
    <ConstraintSet
        android:id="@+id/end2"
        motion:deriveConstraintsFrom="@id/start2">

        <Constraint android:id="@id/button3">
            <Layout
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:layout_marginEnd="8dp"
                motion:layout_constraintEnd_toEndOf="parent" />
        </Constraint>
    </ConstraintSet>
</MotionScene>
